import { h } from 'hyperapp'

const Auth = () => (
  <section class="flex justify-center items-center" id="auth">
    <button class="container fill-purple w-64 h-32 flex justify-center items-center text-white text-xl m-4">
        Log in with Discord
    </button>
  </section>
)

export default Auth
